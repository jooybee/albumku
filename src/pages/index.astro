---
import Layout from '../layouts/Layout.astro';
import JoinScreen from '../components/JoinScreen';
import { EVENT_CONFIG } from '../lib/config';
---

<Layout title={`${EVENT_CONFIG.name}`}>
  <JoinScreen client:only="react" />
</Layout>
